<article class="user-card flex justify-between items-center bg-[#E2CBFF] py-2 px-4 rounded">
  <h3 class="font-bold text-lg">{{username}}</h3>
  {{#if creator}}
  <button class="delete-btn py-2 px-4 rounded font-bold bg-gray-300 hover:bg-red-500" id="{{id}}">Remove</button>
  {{/if}}
</article>